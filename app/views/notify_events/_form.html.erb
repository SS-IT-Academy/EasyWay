<%= form_for(@notify_event) do |f| %>
  <% if @notify_event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notify_event.errors.count, "error") %> prohibited this notify_event from being saved:</h2>

      <ul>
      <% @notify_event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :template_id %><br />
    <%= f.number_field :template_id %>
  </div>
  <div class="field">
    <%= f.label :event_id %><br />
    <%= f.number_field :event_id %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :observer_id %><br />
    <%= f.number_field :observer_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
