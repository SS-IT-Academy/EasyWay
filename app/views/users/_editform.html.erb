<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
 <div class="field">
    <%= f.label :username %><br />
    <%= f.text_field :username %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">	
  	<%= f.label 'new password: ' %><br/>
    <%= f.password_field :password %>
	</div>
	<div class="field">	
  	<%= f.label 'confirm new password:' %><br/>
    <%= f.password_field :password_confirmation %>
	</div>
  </div>
 	<%= select_tag :roles_id, options_from_collection_for_select(Role.all, :id, :name) %>
  </div>
  <div class="actions">
    <%= f.submit 'Update'%>
  </div>
<% end %>
