
<%= form_for @table_template, :html => {:class=>"form-horizontal"} do |f| %>
  <% if @table_template.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@table_template.errors.count, "error") %> prohibited this table_template from being saved:</h2>

      <ul>
      <% @table_template.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="control-group">
    <%= f.label :name, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :name %>
    </div>   
  </div>
  
  
  <div class="control-group">
    <%= f.label :cell_body, :class => "control-label" %>
    <div class="controls">
      <%= f.text_field :cell_body %>
    </div>
  </div>
  <div class="actions controls">
    <%= f.submit :class => "btn"%>
  </div>
<% end %>
