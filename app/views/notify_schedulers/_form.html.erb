<%= form_for(@notify_scheduler) do |f| %>
  <% if @notify_scheduler.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@notify_scheduler.errors.count, "error") %> prohibited this notify_scheduler from being saved:</h2>

      <ul>
      <% @notify_scheduler.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :period %><br />
    <%= f.number_field :period %>
  </div>
  <div class="field">
    <%= f.label :start_at %><br />
    <%= f.time_select :start_at %>
  </div>
  <div class="field">
    <%= f.label :end_at %><br />
    <%= f.time_select :end_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
