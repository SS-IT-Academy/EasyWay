<h1>Listing event_resources</h1>

<table>
  <tr>
    <th>Resource</th>
    <th>Event</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @event_resources.each do |event_resource| %>
  <tr>
    <td><%= event_resource.resource_id %></td>
    <td><%= event_resource.event_id %></td>
    <td><%= link_to 'Show', event_resource %></td>
    <td><%= link_to 'Edit', edit_event_resource_path(event_resource) %></td>
    <td><%= link_to 'Destroy', event_resource, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Event resource', new_event_resource_path %>
